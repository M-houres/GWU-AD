# 算法训练资料更新记录

更新日期：2026-04-10

## 本轮盘点结论

- 统一资料根目录仍为 `C:\Users\m\Desktop\001项目\算法训练资料包`
- 本轮明显变化集中在 `知网资料包`
- `维普资料包` 本轮总量未变化

## 最新库存快照

### 知网

- 总文件数：`66`
- 扩展名分布：`.pdf=47`、`.docx=14`、`.zip=5`
- 角色分布：
  - `aigc_report=28`
  - `dedup_report=24`
  - `rewrite_doc=8`
  - `source_doc=5`
  - `other=1`

对比上一轮记录：

- 总量从 `30 -> 66`，净增 `36`
- `aigc_report` 从 `10 -> 28`，净增 `18`
- `dedup_report` 从 `7 -> 24`，净增 `17`
- `rewrite_doc` 无增长
- `source_doc` 无增长

结论：

- 这次新增几乎全部是知网官方结果侧资料，主要价值在于结果拟合、规则提炼、段落高亮规律归纳
- 需要修正一点：知网 `全文报告` 本身包含 `原文内容`，可提取出大段正文，因此它不是“纯结果壳子”，而是可作为 `弱正文源`
- 但 `全文报告` 仍不等价于原始 `docx`，因为其中混有页眉、检测字段、分页、标注和格式噪声
- 所以这批资料对严格 benchmark 扩容的帮助，比我上一轮判断更大；只是进入正式评分集前，仍应先做去噪、标题映射和正文切片校正

### 维普

- 总文件数：`38`
- 扩展名分布：`.zip=37`、`.html=1`
- 角色分布：
  - `dedup_report=37`
  - `other=1`

结论：

- 维普资料池本轮无新增，但仍然是 `vip_dedup` 的主力训练池
- 其中多版本标题组依旧有价值，适合做“同题多版本改写效果差异”分析

## 对 6 个算法包的影响

### 增益明显

- `cnki_aigc_detect`
  - 受益最大
  - 可显著补强知网 AIGC 结果结构、疑似段落、高亮片段的规则提炼
- `cnki_dedup`
  - 受益明显
  - 可继续补知网查重高风险片段、对照结构和简洁报告字段映射

### 增益有限但强于上一轮判断

- `cnki_rewrite`
  - 本轮没有新增同等强度的“原文 / 改写稿 / 改写后平台结果”成套样本
  - 但若某些全文报告可稳定回抽原文正文，则可部分增强“改写前平台结果”侧的对照价值
  - 结论仍是：规则提炼提升明显，量化评分池提升有限

### 本轮基本无变化

- `vip_aigc_detect`
- `vip_dedup`
- `vip_rewrite`

## 本轮最有价值的判断

1. 知网资料现在已经从“只有少量样本”进入到“有一批可持续提规则的报告池”阶段。
2. 这批新增最适合喂给 `cnki_aigc_detect` 和 `cnki_dedup`，同时也能作为 `弱正文源` 反哺严格 benchmark 整理。
3. 虽然 `source_doc` 没增长，但 `全文报告` 自带原文内容，所以知网 benchmark 扩容不再只依赖原始 `docx`；只是质量分级上要区分“原始正文源”和“报告回抽正文源”。
4. 维普这轮不用重做判断，继续维持“`vip_dedup` 最强、`vip_aigc_detect` 和 `vip_rewrite` 偏弱”的结论。

## 建议的后续动作

1. 先把本轮新增知网 PDF 结果全部纳入规则抽取池。
2. 将可唯一映射到标题的知网 AIGC / 查重全文报告优先整理成候选 benchmark 清单。
3. 将全文报告回抽出来的正文标记为“弱正文源”，与原始 `docx` 分开管理。
4. 后续继续补：
   - 知网同标题原文
   - 知网改写前后成套样本
   - 维普 AIGC 样本

## 本轮产出

- 标准盘点 JSON：`logs/algo_workspace_inventory.latest.json`
- 标准盘点 Markdown：`logs/algo_workspace_inventory.latest.md`
